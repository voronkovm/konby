{
  "name": "konby",
  "version": "0.1.0",
  "description": "Konby CLI for board bootstrap and local setup",
  "scripts": {
    "test": "node --test \"test/unit/**/*.test.js\"",
    "test:integration": "node --test \"test/integration/**/*.test.js\"",
    "test:all": "node --test \"test/**/*.test.js\"",
    "test:coverage": "c8 node --test \"test/unit/**/*.test.js\"",
    "test:coverage:all": "c8 node --test \"test/**/*.test.js\""
  },
  "c8": {
    "all": true,
    "include": ["lib/**/*.js", "bin/*"],
    "extension": [".js", ""],
    "reporter": ["text"],
    "temp-directory": "/tmp/konby-coverage"
  },
  "bin": {
    "konby": "bin/konby"
  },
  "files": [
    "bin",
    "lib",
    "presets",
    "README.md"
  ],
  "license": "UNLICENSED",
  "devDependencies": {
    "c8": "^11.0.0"
  }
}
